import React from 'react';
import { RegistrationPage } from '@/views/auth';

const Page = () => {
  return (
    <div className='container mx-auto flex justify-center items-center h-screen'>
      <RegistrationPage />
    </div>
  );
};

export default Page;
